// Floating chat FAB removed — chats live in the bottom nav; the single
// floating icon is now the notifications bell (components/layout/floating-bell.tsx).
export function QuickActions() {
  return null;
}
