// Testing period: the web app offers ONLY Telegram login/registration. Phone +
// password login and the multi-step phone/OTP registration are hidden behind
// this flag (not deleted). Flip to `false` to restore them.
export const AUTH_TELEGRAM_ONLY = true;
